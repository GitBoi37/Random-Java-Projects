public void insertSort(){
        if(index < sorts.length){
            for(int i = 0; i < index; i++){
                int jeff = sorts[index];
                if(sorts[i] > jeff){
                    for(int j = i; j < sorts.length - 1; j++){
                        
                    }
                    index++;
                }
            }
        }
    }
