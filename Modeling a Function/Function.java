import java.util.*;
class Function {
  ArrayList<Term> terms;
  public Function(int length, Term[] terms){
    for(int i = 0; i < terms.length; i++){
      this.terms.add(terms[i]);
    }
  }
  public takeDerivative
}
